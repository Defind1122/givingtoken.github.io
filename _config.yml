# Site settings
title: Box Token
email: your-email@domain.com
url: https://boxtoken.github.io/
description: "Boxtoken is ethereum smart contract that is liked by many internet users."

# Color settings (hex-codes without the leading hash-tag)
color:
  primary-dark: '#000000'
  links: '#219ab3'

# Footer settings
footer: BoxToken

# Build settings
markdown: kramdown
permalink: pretty
